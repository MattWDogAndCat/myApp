import { iTheme } from "componet/model/interface";

const lightTheme: iTheme = {
  custom: {
    width: 300,
    height: 100,
  },
  palette: {
    background: {
      paper: "none",
      default: "#F7F7F7",
    },
    mode: "light",
  },
};

export default lightTheme;
